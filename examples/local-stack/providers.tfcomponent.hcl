# No cloud provider needed — components use the built-in terraform_data resource.
# (This file exists so the stack has the expected shape; stack2tf reads provider
# wiring from each component's providers map, which is empty here.)
